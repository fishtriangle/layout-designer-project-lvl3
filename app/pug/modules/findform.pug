form(class="mb-5")
  .form-group(class="border-bottom border-gray-base")
    label.sr-only(for="search")= findForm.formName
    input.form-control(type="text", placeholder= findForm.formText class="p-4 border-0 shadow-none bg-white")
