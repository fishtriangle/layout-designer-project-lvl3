form(class="mb-5")
  .form-group(class="border-bottom border-gray-base")
    label.sr-only(for="search") Контакты
    input.form-control(type="text", placeholder="Поиск по контактам" class="p-4 border-0 shadow-none bg-white")
